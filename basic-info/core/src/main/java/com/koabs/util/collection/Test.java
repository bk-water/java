package com.koabs.util.collection;

import java.util.*;

/**
 * Author: koabs
 * 6/22/17.
 */
public class Test {
    public static void main(String[] args) {
        java.util.List list = new ArrayList<>();
        Set set = new HashSet<>();
        set.add(null);
        System.out.println("args = [" + set.size() + "]");
        TreeMap treeMap = new TreeMap();
        Iterator iterator = new HashMap<>().entrySet().iterator();
        list.iterator();
    }
}
